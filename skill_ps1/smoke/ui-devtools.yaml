name: smoke-ui-devtools
version: 1.0
purpose: Verify local UI page opens and optional devtools trigger works.

inputs:
  required: [url]
  optional: [browser, open_devtools]

defaults:
  browser: edge
  open_devtools: false

pass_criteria:
  - browser_process_started
  - url_open_command_success
